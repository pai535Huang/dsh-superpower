# DSH bundle layer: mount the host plugin that registers the Superpowers
# skills into the global skill layer and restores the session-start bootstrap
# for any agent whose tool view includes the `skill` tool. The plugin ships no
# preset; every preset with a skill-loading surface picks the skills up
# automatically.
- insert:
    - id: dsh-superpower
      name: dsh-superpower
